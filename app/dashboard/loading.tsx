import { PageSkeleton } from "@/app/components/ui/skeleton";

/**
 * Shown by Next.js while a dashboard route segment is loading.
 * Replace with a route-specific skeleton when needed.
 */
export default function DashboardLoading() {
  return <PageSkeleton />;
}
